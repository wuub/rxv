[tox]
envlist = py37,py38,py39
skip_missing_interpreters = True

[testenv]
setenv =
    LANG=en_US.UTF-8
    PYTHONPATH = {toxinidir}
deps =
     -r{toxinidir}/test-requirements.txt
commands=py.test tests --timeout=30 --cov=rxv --cov-report html {posargs}
